// All required test dependencies are configured in the root build.gradle.kts:
// - JUnit 5 (org.junit.jupiter:junit-jupiter)
// - kotlin-test
// - kotlinx-coroutines-test
// - Test task configured to use JUnit Platform

dependencies {
    // No additional dependencies needed - all test dependencies are inherited from root project
}
